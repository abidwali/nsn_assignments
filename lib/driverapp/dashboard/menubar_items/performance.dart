import 'package:flutter/material.dart';
import 'package:nsn_assignments/driverapp/widgets/custom_text.dart';

class PerformanceMenu extends StatelessWidget {
  const PerformanceMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextTwo(text: "Performance Under Process"),
      ),
    );
  }
}
