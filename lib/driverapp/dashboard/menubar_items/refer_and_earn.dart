import 'package:flutter/material.dart';
import 'package:nsn_assignments/driverapp/widgets/custom_text.dart';

class ReferAndEarnMenu extends StatelessWidget {
  const ReferAndEarnMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextTwo(text: "Refer And Earn Menu Under Process"),
      ),
    );
  }
}
