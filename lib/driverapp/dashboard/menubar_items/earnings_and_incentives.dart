import 'package:flutter/material.dart';
import 'package:nsn_assignments/driverapp/widgets/custom_text.dart';

class EarningAndIncentives extends StatelessWidget {
  const EarningAndIncentives({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextTwo(text: "Earnings And Incentives Under Process"),
      ),
    );
  }
}
