import 'package:flutter/material.dart';
import 'package:nsn_assignments/driverapp/widgets/custom_text.dart';

class MessageMenu extends StatelessWidget {
  const MessageMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextTwo(text: "Message Page Under Process"),
      ),
    );
  }
}
